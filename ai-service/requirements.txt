# ─── Web Framework ────────────────────────────────────────────────────────────
fastapi==0.115.5
uvicorn[standard]==0.32.1
python-multipart==0.0.12
httpx==0.27.2

# ─── Deep Learning ────────────────────────────────────────────────────────────
# PyTorch stack (official MambaVision implementation is PyTorch-first)
torch==2.4.1
torchvision==0.19.1

# Official Hugging Face loader for MambaVision pretrained weights
# NOTE: The 'mambavision' pip package is NOT listed here — it requires the
# CUDA-only 'mamba-ssm' kernel which is unavailable on Windows/CPU.  The
# application loads MambaVision exclusively via
#   transformers.AutoModelForImageClassification.from_pretrained(
#       "nvidia/MambaVision-T-1K", trust_remote_code=True
#   )
# which only requires the packages below.
transformers==4.50.0
safetensors==0.4.5
timm==1.0.15        # required by the nvidia/MambaVision HF model code
einops==0.8.1       # required by the nvidia/MambaVision HF model code

# ─── Computer Vision ──────────────────────────────────────────────────────────
opencv-python-headless==4.10.0.84
Pillow==11.0.0
scikit-image==0.24.0        # GLCM texture feature extraction (Eq. 8–14)

# ─── Numerical / Data ─────────────────────────────────────────────────────────
numpy==2.1.3
pandas==2.2.3
scipy==1.14.1

# ─── Visualisation ────────────────────────────────────────────────────────────
matplotlib==3.9.3

# ─── Machine Learning / Metrics ───────────────────────────────────────────────
scikit-learn==1.5.2

# ─── Configuration / Environment ──────────────────────────────────────────────
python-dotenv==1.0.1
pydantic==2.10.3
pydantic-settings==2.6.1

# ─── Logging / Monitoring ─────────────────────────────────────────────────────
loguru==0.7.3
psutil==6.1.0                      # system resource monitoring (CPU/RAM/disk/GPU)

# ─── Security / Authentication ────────────────────────────────────────────────
python-jose[cryptography]==3.3.0   # JWT encode / decode
passlib[bcrypt]==1.7.4             # password hashing (bcrypt)
bcrypt==4.0.1                      # explicit bcrypt pin (passlib backend)
slowapi==0.1.9                     # rate limiting (Starlette-compatible)
limits==3.13.0                     # slowapi backend

# ─── Testing ──────────────────────────────────────────────────────────────────
pytest==8.3.3
pytest-asyncio==0.24.0
